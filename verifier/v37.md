# v37 — Landscaping mobile service selector fix (2026-09-07)

Client brief ("Step 6 — Fix Landscaping mobile service tabs"): the "What we
handle" selector (01 Turf / 02 Beds / 03 Pruning / 04 Seasonal / 05
Maintenance) was a horizontal scroll rail on mobile that clipped later
items. Replace with a mobile layout that fits the viewport with every
category reachable — no horizontal overflow, no clipped labels. Content,
categories, images, and desktop unchanged.

## Change (1 file)

components/services/CapabilityExplorer.tsx — the selector <ul> is now a
vertical full-width ledger on mobile (flex-col, ruled rows, same as the
desktop column) instead of a snap-x overflow-x-auto rail. Redundant
mobile-only rail classes removed (snap/overflow/shrink); desktop column
behavior identical. Images, details, and the cross-fade swap untouched.

## Validation (production build, rendered)

- tsc clean; eslint 0 errors; build OK.
- Rendered @390 and @320: selector stacks vertically, all 5 categories
  fully inside the viewport (allFit), list not horizontally scrollable,
  page overflow 0.
- Interaction: tapping "Maintenance" at mobile and desktop cross-fades the
  photo (land-field-sod → land-story-installed) and updates the label.
- Screenshots @390 (stacked ledger + swapped photo + label below image)
  and @1440 (desktop column + overlaid caption panel unchanged).
- Full sweep: 126/126 (14 routes × 9 widths), 0 overflow, 0 errors.

Evidence: tests/results/v37-mobile-selector/.
