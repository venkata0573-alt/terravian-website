# v40 — Site-wide brand system & white-background cleanup (2026-09-08)

Client brief: "TERRAVIAN — SITE-WIDE BRAND SYSTEM & WHITE-BACKGROUND CLEANUP" — use the
Terravian logo as the visual reference for the entire website. Explicitly a consistency
pass, NOT a structural redesign: no routes, structure, section order, images, videos,
responsive behavior, hero fixes, mobile sticky CTA, forms, or metadata changes.

Official system: GREEN #2F6D4D (primary brand) / BROWN #7C500C (secondary accent) /
PURE WHITE #FFFFFF backgrounds / charcoal for informational text only.

NOTE: this brief sets the GLOBAL system and intentionally overrides v39's per-area
eyebrow colors: eyebrow/small labels → BROWN, major branded headings → GREEN, body →
CHARCOAL. v39's explicit examples are reversed accordingly (WHY TERRAVIAN brown + green
headline, per this brief's own worked example).

## 1. Global background rule — pure white
- Body: `bg-paper` (#FCFCF9) → `bg-white`. All page sections now render on pure white.
- Removed tinted section bands: promise section `bg-stone2` removed; Our Work + In Good
  Company sections `bg-white` redundant classes removed; FieldMarquee rail `bg-stone2`
  removed (kept its hairline border-y separators); ProofOfWork `bg-stone2` removed;
  HardscapeShowcase spaces section `bg-paper` removed; WhyTerravian cards `bg-white`
  removed (hairline-bordered cards on the white ground).
- Footer: warm `#FAF9F5` → `bg-white` (brief §8: footer must be PURE WHITE).
- Chrome touched for consistency: mobile nav drawer cream→white, mobile action-bar RFP
  half cream→white (structure untouched), intro loader cream→white, ServiceCards tag
  pill cream→white, PortfolioGrid service tags bg-stone2→border-only.
- Kept: form status/highlight fills (bg-tint inside forms — functional), MaintenanceCalendar
  data-cell tints (informational data visualization, borders separated), image-well
  placeholders behind photographs, on-photo scrims/badges (unavoidable functional reason —
  legibility on photography).

## 2. Dark sections removed (3 full-bleed charcoal bands → white)
- How We Work "Short version" band: bg-charcoal → transparent/white; eyebrow tint→brown;
  heading cream→green; serif statements cream→charcoal; lines cream/70→charcoal/70;
  separators cream/15→charcoal/15.
- Asphalt "Restoration & reconstruction" section: bg-charcoal → white; gold eyebrow →
  brown; heading cream→green; body cream/90→charcoal/90; Restoration/Reconstruction h3s
  gold→green; bullets gold→green; caption cream/70→charcoal/70.
- Case study "Services delivered" ledger: bg-charcoal → white; gold eyebrow → brown;
  heading cream→green; ledger rules cream/15→earth/30 with earth hover; numerals
  cream/40→earth/70; service names cream/90→charcoal.
- Hardscape centerpiece article: bg-charcoal → white; gold eyebrow → brown; title
  cream→green; body cream/85→charcoal/85; caption gold-bar→earth-bar, cream/70→charcoal/70.
- Case study closing CTA band: bg-cream → white with hairline top rule; primary button
  was invisible-on-cream (bg-cream) → proper primary (bg-forest text-cream, forest-dark
  hover); secondary outline button unchanged.
- MaintenanceCalendar sticky header row: bg-charcoal → bg-forest (functional table header,
  brand-colored instead of black).

## 3. Heading rule — brown eyebrow / green heading / charcoal body
- Promise section: eyebrow green→brown; heading charcoal+green-em-split → ALL green serif
  ("The Company That Shows Up." — the brief's literal example); links stay brown.
- In Good Company: eyebrow green→brown; heading charcoal+"proudly"-em-split → all green
  (via SectionHeading default text-charcoal→text-forest — single consumer).
- Our Work: eyebrow green→brown; heading charcoal+"speaks"-em-split → all green serif.
- Divisions heading: charcoal → green; intro upgraded to the italic serif editorial style
  (premium brand moment, serif preserved on desktop after mobile-sans media query).
- Portfolio: eyebrow green→brown; h1 → green; card category labels green→brown (secondary
  metadata = brown per system); filters already compliant (selected green, neutral rest).
- Service template sections (story/handle/snow-video/gallery/related h2s): charcoal → green;
  standard-template h1 → green.
- Informational page h1s (About, Contact, Industries, Request a Proposal, Privacy,
  Services index, 404): charcoal → green ("black must not dominate major headings").
- ProgramsIndex h2 → green. All eyebrows site-wide are brown by default (`.eyebrow`
  = text-earth, unchanged).
- No mixed-color word-by-word headings remain (em splits removed; verified count = 0).

## 4. Buttons
Primary = green/white, secondary = white/green-border — already the system (Button.tsx);
the one non-compliant button (invisible cream-on-cream case-study CTA) fixed to primary
green. On-photo outline controls (hero Call button, carousel) unchanged — on-photography
treatment, not page CTAs.

## 5. Nav / footer / cards
- Nav already compliant: inactive charcoal, active green underline, RFP green button.
- Footer: white bg; headings green (consistent hierarchy), links charcoal with green
  hover, contact icons green — verified rendered.
- Cards: white/hairline-bordered; selected/active states green; metadata brown; titles
  charcoal-or-green per hierarchy.

## 6. Untouched (per brief §11)
Routes, structure, section order, images/image order, videos, responsive behavior, hero
crops, mobile hero cleanup, sticky mobile CTA, asphalt media alignment, forms, metadata,
content wording. On-photo text/scrims, photo-container backgrounds, green buttons/pills,
and dead code (SnowCommand — unreferenced) left as-is.

## Gates
- tsc --noEmit: clean. ESLint: 0 errors (3 pre-existing warnings — baseline).
- Production build OK.
- Rendered verification (Playwright computed styles + geometry, 41 checks): body white;
  ZERO section/article/footer with non-white background across pages; promise/IGC/our-work/
  portfolio eyebrows brown, headings green, no em splits; footer white with green headings,
  charcoal links; ShortVersion/asphalt-restoration/services-delivered/case-CTA bands white
  with correct hierarchy; calendar header green; CTA primary button green+cream text;
  selected filter green; mobile body white; zero overflow @390.
- Sweep: 126/126 (14 routes × 9 widths) — 200s, zero overflow, zero console/page errors.

Evidence: tests/results/v40-brand-system/ (home-promise, footer desktop+mobile,
hww-short-version, asphalt-restoration, case-services-delivered, case-cta, portfolio,
home-promise-mobile).
