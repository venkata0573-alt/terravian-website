# v41 — Hero overlay removal + official wordmark font (2026-09-08)

Client message (with two uploaded references: a full-page screenshot showing the Call
button on the homepage hero, and the official text-only wordmark on white):

1. "Remove the call from the hero image — there should be nothing on the hero images,
   it should be clean and bright; all the hero images should be the same so the client
   sees visually good."
2. "The header name of Terravian Landscaping is wrong — it should be in this format as
   in image 2 and font is Cinzel Bold."

## 1. Homepage hero — nothing over the photography
- Removed the desktop Call 475-347-4090 overlay strip (button + charcoal/45 backing
  panel) from HeroRotator.tsx; unused PhoneIcon import removed.
- Result: every slide now carries ZERO overlay content — no buttons, no links, no text
  (verified: 0 visible links in the hero region on desktop and mobile). The only
  remaining chrome is the carousel navigation itself: counter, prev/pause/next arrows
  (desktop) and slide dots (mobile), which the client explicitly keeps.
- All four slides render identically clean and bright; no per-slide differences.
- sr-only h1 retained for SEO/a11y. Fixed mobile Call Now / Request a Proposal bar
  untouched (it is site chrome, not on the hero image).

## 2. Header wordmark — Cinzel Bold
- Downloaded Cinzel Bold (700) TTF from Google Fonts, converted to woff2 (19.8KB),
  self-hosted at public/fonts/cinzel-bold.woff2, loaded via next/font/local as
  `--font-cinzel` (OFL-licensed, no CDN), new Tailwind family `font-wordmark`.
- SiteHeader wordmark spans switched from Newsreader semibold to Cinzel Bold caps:
  TERRAVIAN green #2F6D4D over LANDSCAPING brown #7C500C, both lines at equal visual
  width (1.375rem / 0.875rem + 0.205em tracking) — matching the supplied reference
  format. `.brand-serif` retained on both spans so mobile serif exemption still applies.
- Footer tagline remains Newsreader italic (brand accent, untouched).

## Gates
- tsc --noEmit: clean. ESLint: 0 errors (baseline warnings unchanged).
- Production build OK.
- Rendered verification: hero has 0 overlay links desktop + mobile, carousel controls
  retained (7 buttons/dots), wordmark computed font-family = cinzel stack, weight 700,
  TERRAVIAN rgb(47,109,77), LANDSCAPING rgb(124,80,12); document.fonts confirms
  "cinzel 700 loaded" and the stack measures wider than Georgia (real font applied);
  no horizontal overflow @390.
- Sweep: 126/126.

Evidence: tests/results/v41-hero-wordmark/ (hero desktop+mobile, header desktop+mobile).
