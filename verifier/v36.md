# v36 — Mobile typography cleanup (2026-09-07)

Client brief ("Step 5 — Mobile typography cleanup"): on mobile only, reduce
serif usage across service pages — sans for headings, service titles, body,
buttons, labels, tabs; serif only where it is a natural brand accent; no new
font family; desktop typography system unchanged.

## Changes (4 files)

1. app/globals.css — one media query (max-width: 767.98px): any
   .font-display element without .brand-serif falls back to the existing
   site sans stack ("Helvetica Neue", Helvetica, Arial, system-ui). One
   rule covers every serif heading/label on the service pages (asphalt
   level headings + captions, hardscape showcase chapters, FAQ "Answered
   before you ask", landscaping capability names, etc.). Desktop rule
   untouched.
2. app/services/[service]/page.tsx — snow figure captions ("Storm watch" /
   "Clear access" / "Documented") font-display → font-semibold; FAQ
   question text font-display → font-semibold (they read as labels, not
   editorial accents).
3. components/layout/SiteHeader.tsx — wordmark spans tagged .brand-serif
   (kept serif on mobile as the brand mark).
4. components/layout/SiteFooter.tsx — tagline "The Company That Shows Up."
   tagged .brand-serif (kept serif italic on mobile as the brand accent).

No new font family introduced; body text was already sans; buttons/tabs
were already sans.

## Validation (production build, rendered, computed fonts)

- tsc clean; eslint 0 errors; build OK.
- @390 on all 4 service pages: every .font-display element computes to the
  sans stack EXCEPT the header wordmark and footer tagline (brand-serif,
  still Newsreader); FAQ questions compute sans.
- @1440: full serif system intact — asphalt/hardscape/landscaping serif
  headings all still Newsreader; identical to pre-change.
- Screenshots reviewed @390 (sans FAQ ledger + brand serif tagline) and
  @1440 (serif rail headings unchanged).
- Full sweep: 126/126 (14 routes × 9 widths), 0 overflow, 0 errors.

Evidence: tests/results/v36-mobile-typography/.
