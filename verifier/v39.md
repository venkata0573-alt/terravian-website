# v39 — Brand color, typography & visual cleanup pass (2026-09-08)

Client brief: "TERRAVIAN — BRAND COLOR, TYPOGRAPHY & VISUAL CLEANUP PASS" with 7 reference
screenshots. Explicitly NOT a redesign — no structure, photography, section-order, layout,
responsive-behavior, or previous-fix changes. Goals: disciplined brand colors, removed
labels/redundancy, simplified mobile imagery.

## Brand green unification
Client declared GREEN #2F6D4D / BROWN #7C500C the official site colors for this pass and
QC'd "#2F6D4D is the consistent Terravian green". Implemented as a single-token update:
`forest.DEFAULT` #2C6848 → #2F6D4D and derived hover `forest.dark` #245A3E → #235039
(same hue/sat, −8% lightness, verified via HLS conversion). No hardcoded instances of the
old green existed outside the token (grep-verified). Brown token unchanged (#7C500C).
Tint (#E4EFE4) and on-photo gold (#E4B263) untouched — not decorative accents.

## Image 1 — "Why Terravian / The Company That Shows Up" (homepage §6)
- Eyebrow "Why Terravian": earth(brown) → forest green (utility override on `.eyebrow`).
- Headline unchanged: "The Company That" charcoal, "Shows Up." green italic serif (already correct).
- Body unchanged (charcoal/85).
- Links "See how we work" / "Meet the company": green → BROWN #7C500C including 2px
  underline and arrow (inherits text color); hover → earth-dark (stays in the brown family).
- Wording, fonts, sizes, spacing, layout untouched.

## Image 2 — "In good company / Properties we proudly serve" (homepage §5)
- Eyebrow → forest green via new optional `eyebrowClassName` prop on SectionHeading
  (default behavior unchanged; SectionHeading has exactly one consumer).
- Headline already correct: "Properties we"/"serve" charcoal, "proudly" green italic serif.
- Intro already charcoal. Logo rail untouched (no recolor, no carousel/layout change).

## Images 3/4 — "Our work / The work speaks first" (homepage §4 + PortfolioFeature)
- Green "FEATURED" badge on the Hilton/DoubleTree feature photo REMOVED (span deleted;
  no replacement). Image, title, gold category label, link, and grid untouched.
- Eyebrow "Our work": brown → green.
- Headline: all-green → charcoal with a single green italic accent word ("speaks"),
  matching the established house accent pattern ("proudly", "Shows Up.").
- Handoff link "Browse the full portfolio": green → brown w/ earth-dark hover (brown
  reserved for restrained links per the brief's hierarchy). Brown editorial bar on the
  supporting copy retained as the section's small secondary accent.

## Image 5 — Portfolio page
- Eyebrow "Portfolio": brown → green. H1 already charcoal; body already charcoal/90.
- "Demonstration content:" lead-in: brown → charcoal (body-text rule), still bold.
- Card category labels (Hotel & Hospitality etc.): brown → green (eyebrow/small-label rule).
- Filters verified already compliant: selected = green fill, unselected = white/neutral,
  green only as hover accent. Cards, images, tags, links not redesigned.

## Image 6 — Mobile service image cards (DivisionCards, homepage divisions)
- Mobile only (<768px): the one-line descriptive paragraph over each photo hidden
  (`max-md:hidden`); content block height becomes auto (`max-md:h-auto`) so nothing is
  clipped. Cards now show service name + short "SEE X →" label only — photography dominant.
- Desktop card content byte-identical (description verified visible @1440).
- No image file, order, destination, or link changes. Zero horizontal overflow @390.

## Image 7 — Homepage hero
- "Request a Proposal" overlay stays removed (v38 Step 9) — brief confirms: no replacement CTA.
- Phone/call control RESTORED on desktop (client: "Keep the phone/call control for now") —
  the pre-v38 ghost `Call 475-347-4090` button on its slim translucent strip, bottom-left.
  PhoneIcon import restored. Mobile hero unchanged (no overlay; sticky bar carries conversion).
- Carousel counter, prev/pause/next arrows, mobile dots all retained; hero image untouched.

## Global hierarchy result
Charcoal = primary text/headings; green #2F6D4D = brand emphasis (eyebrows, one accent
word); brown #7C500C = restrained links/secondary accents. No heading mixes three colors.

## Gates
- tsc --noEmit: clean. ESLint: 0 errors (3 pre-existing warnings, unchanged baseline).
- Production build OK (.next/standalone/server.js present).
- Rendered verification (Playwright, computed styles + geometry): 47/47 checks —
  exact rgb assertions for every recolored element @1440; FEATURED badge count = 0;
  hero strip = Call only, RFP absent; 4/4 mobile cards name+label only, nothing clipped;
  desktop division descriptions intact; portfolio filters/category colors correct.
- Sweep: 126/126 (14 routes × 9 widths) — 200s, zero overflow, zero console/page errors.

Evidence: tests/results/v39-brand-color-pass/ (7 screenshots: promise, in-good-company,
our-work, hero, divisions desktop+mobile, portfolio).
