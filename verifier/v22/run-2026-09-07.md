# v22 Run Record — Four Service Divisions

Date: 2026-09-07
Criteria: verifier/v22/criteria.md

## Implementation

- `types/project.ts` — `ServiceSlug` gains `"asphalt-pavement"`.
- `content/services.ts` — division 3 renamed to "Patio & Hardscape
  Design-Build" (slug/route unchanged); new fourth entry "Asphalt &
  Pavement Services" (slug `asphalt-pavement`); landscaping tagline
  reworded from "under one crew" to "under one company".
- `content/serviceDetails.ts` — full asphalt entry: 6 process steps,
  labeled-placeholder gallery (existing `portfolio-*-placeholder.webp`),
  5 FAQs, all five industry anchors. Header comment updated.
- `components/home/DivisionCards.tsx` — fourth card; grid rebalanced to
  stacked (mobile, 16/10) → 2×2 (tablet, 4/3) → 4-across (xl, 3/4).
- `components/services/ServiceCards.tsx` — fourth card; grid rebalanced
  to sm:2-col → xl:4-col; landscaping line reworded to "one partner".
- `public/images/placeholders/service-asphalt-card.webp` — NEW labeled
  placeholder ("COMING SOON — Terravian asphalt photography on the
  way"), label constrained to the central crop window so it survives the
  3/4 desktop card crop. Slot documented in IMAGES.md.
- Messaging (§11): homepage eyebrow "One company. Every season.", title
  "Complete property care — one accountable partner.", intro now
  "…patios and pavement in between."; meta descriptions updated on
  /, /services, /about, /contact, /request-a-proposal, layout default;
  sr-only H1 and hero slide label updated; CaseStudy "handled by one
  crew" → "handled by one company". Snow-specific "One crew for the
  whole storm" intentionally kept.
- `components/home/MaintenanceCalendar.tsx` — fifth row "Asphalt &
  Pavement" (Mar planning, Apr active, May–Sep peak, Oct active,
  Nov planning); caption and comments updated (5 division rows).
- `content/industries.ts` — `asphalt-pavement` added to all five
  `relevantServiceSlugs` arrays.
- Auto-verified consumers: nav (5 asphalt-pavement refs in homepage
  HTML), footer, sitemap.xml (1 entry), service JSON-LD (correct
  name/url), proposal-form checkbox options (label present, escaped),
  `/request-a-proposal?service=asphalt-pavement` pre-select path.
- Not changed (deliberate): PortfolioGrid filters (no asphalt projects
  exist — an empty filter would look broken); portfolio meta (describes
  current demonstration categories); hero slide set (photography-led, no
  asphalt photo supplied).

## Results

- `npx tsc --noEmit`: clean.
- `npm run lint`: 0 errors, 3 pre-existing deliberate-`<img>` warnings.
- `npm run test:unit`: 20/20 pass.
- `npm run build`: success; `/services/asphalt-pavement` prerendered.
- Rendered-HTML grep across 8 routes: zero occurrences of "one crew"
  (outside the intentional snow line), "Lawns, snow", or "three
  division(s)".
- Responsive sweep: 14 routes × 9 widths = 126/126 pass (status 200, no
  horizontal overflow, no console/page errors) —
  tests/results/v22-divisions/sweep-results.json.
- Visual validation (screenshots in tests/results/v22-divisions/):
  - Divisions band at 1440/1366/768/390: four balanced cards, asphalt
    placeholder label fully legible at every crop, CTA spacing intact.
  - /services at 1440/768/390: 4-across → 2×2 → stacked, equal weight.
  - /services/asphalt-pavement full-page at 1440 and 390: complete
    generic template (intro, capabilities, who-it's-for + industry
    chips, labeled gallery, 5 FAQs, related-projects fallback note,
    CTA, footer with all four divisions).
  - Homepage full-page at 1440/768/390: new eyebrow/title/intro render;
    no old three-service messaging.

## Known limitations

- Asphalt card/gallery imagery is a labeled placeholder per standing
  integrity rules; real photography slot documented in IMAGES.md.
- The asphalt service page uses the standard (non-personality) template;
  a photographic hero can be added when approved photography exists.
