# v25 — Run Record — 2026-09-07

Two-change scoped pass per the client brief ("Make ONLY these two
changes"). Evidence: tests/results/v25-tile-video/.

## Change 1 — Homepage Asphalt & Pavement tile image: PASS

- New client-supplied paving photograph (1536×1024, exactly 3:2) rendered
  to service-asphalt-card-wide.avif (1400×933, 280 KB), replacing the
  previous tile crop at the same path — tile dimensions, text, overlay,
  CTA, typography, and the 2×2 grid untouched.
- The source ratio matches the desktop tile exactly, so the tile renders
  essentially uncropped: yellow roller prominent in the left foreground,
  fresh black mat dominant, loader and truck down the road, blue sky.
  Simulated mobile 16:10 object-cover trim loses only thin sky/foreground
  slivers — roller and fresh asphalt remain fully visible (verified in the
  390px render).
- Alt text updated to describe the new photograph accurately.
- Landscaping, Snow & Ice, and Patio & Hardscape tiles untouched.
- Evidence: d-tile-asphalt.png, m-tile-asphalt.png.

## Change 2 — Hot-mix video moved into Level One: PASS

- The maintenance grid was restructured from [text | media-pair] to a
  12-column two-row layout: text ledger (col-span-5), sealcoating figure
  (col-span-4), tall crack-sealing video (col-span-3, row-span-2 — same
  size and content as before, unmoved on the right), and the hot-mix
  video placed in the second row (col-span-7) filling the previously
  blank area under the text + sealcoating, matching the client's
  red-rectangle reference.
- Video keeps its natural landscape aspect (aspect-[540/304]) at ~700px
  rendered width — intentional and useful without becoming an oversized
  full-width row; whitespace retained to the right.
- The separate interlude section between Level One and Level Two was
  removed; LEVEL TWO — REPAIR now begins with the standard section
  padding (py-16 md:py-24) immediately after Level One — measured
  section flow: maintenance h=1095px → repair, no blank containers.
- Mobile (390px): content stacks in reading order — text → sealcoating →
  crack-sealing video → hot-mix video → repair; no forced desktop
  arrangement, controls usable, no overflow.
- Evidence: d-maintenance.png, d-maint-to-repair.png, d-repair-entry.png,
  m-maintenance-1.png, m-maintenance-2.png.

## Gates

- tsc clean; eslint 0 errors (3 pre-existing warnings); production build
  26/26 prerendered; responsive sweep 126/126 (14 routes × 9 widths, 0
  overflow, 0 failed responses, 0 console/page errors).
- No other pages, components, images, copy, or styling were modified —
  diff touches only components/home/DivisionCards.tsx (one image path
  target + alt), components/services/AsphaltLevels.tsx (maintenance grid
  + interlude removal), and the single new AVIF asset.

## Verdict

Both requested changes verified against the rendered result at desktop
1440 and mobile 390. Nothing else on the site changed.
