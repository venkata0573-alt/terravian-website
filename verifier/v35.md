# v35 — Mobile copy reduction on service pages (2026-09-07)

Client brief ("Step 4 — Mobile copy reduction"): on mobile only, shorten
long explanatory body copy on the four service pages — keep headings,
concise service lists, and proof/documentation content; cut repeated
explanations; ~1 short paragraph per section. Desktop copy unchanged; no
section-order, image, or video changes.

## Approach

Mobile-only trims via `max-md:hidden` spans inside existing paragraphs —
desktop markup and copy byte-identical, no rewrites, no section changes.
Sections already concise on mobile (one short paragraph, list, or ledger)
were left untouched — notably all of the landscaping page, both FAQ
sections, and the snow/hardscape standard blocks.

## Trims (4 files… actually 3)

1. app/services/[service]/page.tsx — snow "Working while it's still
   snowing": mobile keeps "Our crews work continuously through the storm.";
   the second explanatory clause renders desktop-only.
2. components/services/AsphaltLevels.tsx —
   L2 repair: mobile drops the framing sentence ("Potholes… liability
   first…"), keeps the process+documentation sentence and the full
   6-item scope list.
   L3/4 restoration: mobile keeps "milled and overlaid" + the
   written-recommendation promise; the parallel reclaimed/rebuilt sentence
   is desktop-only.
   L4 execution: mobile drops the framing sentence, keeps the
   scope/phasing/photographed/inspected documentation chain.
3. components/services/HardscapeShowcase.tsx — "Rooms without walls" intro:
   mobile keeps the opening sentence; the elaboration ("sequence of
   decisions… four spaces below") is desktop-only.

## Validation (production build, rendered)

- tsc clean; eslint 0 errors; build OK.
- Rendered @390: every kept sentence visible, every trimmed clause hidden
  (5/5 checks across snow/hardscape/asphalt).
- Rendered @1440: all full paragraphs intact — desktop copy unchanged
  (precise span-level check: trimmed spans display:inline, visible).
- Mobile screenshots of the trimmed sections reviewed — single short
  paragraphs, headings and lists intact, no images/videos touched.
- Full sweep: 126/126 (14 routes × 9 widths), 0 overflow, 0 errors.

Evidence: tests/results/v35-mobile-copy/.
