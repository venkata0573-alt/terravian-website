# Verifier v3 — division card scrim reduction
Created: 2026-08-23. Extends v1+v2.
1. Division cards: no full-card scrim, no bottom-band scrim — only a snug text chip
   (bg-charcoal/55, hover /70) behind the title/text/CTA block.
2. Text chip must hug content (w-fit), sit inside the card with padding, never overflow card.
3. v1 layout suite re-run: no overflow, no broken images (spot: home @1920 + @390).
