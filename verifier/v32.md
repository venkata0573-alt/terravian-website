# v32 — Mobile service heroes: clean image only (2026-09-07)

Client brief ("Step 1 — Mobile service heroes only"): on mobile, the four
service-page heroes show only the clean photograph — no eyebrow, headline,
paragraph, CTA, or scrim overlay. Desktop unchanged; sticky Call Now /
Request a Proposal bar kept; no other sections touched.

## Change (1 file)

components/services/ServiceHero.tsx (shared by all four service pages):
- dark scrim (bg-charcoal/45) → max-md:hidden
- "Services" eyebrow, supporting paragraph, CTA button → max-md:hidden
- h1 → max-md:sr-only — visually removed on mobile but stays in the
  accessibility tree, preserving the section's aria-labelledby target,
  heading structure, and SEO (display:none on the section label would
  orphan the landmark name)
- desktop (md+) classes untouched — pixel-identical desktop layout
- snow hero's atmospheric Snowfall layer retained (not text)

## Validation (production build, rendered)

- tsc clean; build OK; eslint 0 errors (pre-existing warnings only).
- Rendered DOM @390 and @375 on all 4 service pages: zero visible overlay
  text elements in the hero; h1 not visibly rendered; hero height 523px
  (62vh) of clean image.
- Desktop @1440 on all 4: eyebrow/headline/paragraph/CTA all present —
  byte-identical layout.
- Screenshots: hero-390-{commercial-landscaping,snow-ice-management,
  hardscape-design,asphalt-pavement}.png show clean images with the sticky
  mobile action bar intact; hero-1440-*.png confirm desktop unchanged.
- Full sweep: 126/126 (14 routes × 9 widths), 0 overflow, 0 errors.

Evidence: tests/results/v32-mobile-heroes/.
