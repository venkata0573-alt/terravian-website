# v38 — Steps 7–10: mobile footer, homepage hero, hero brightness (2026-09-07)

## Step 7 — Mobile footer alignment
SiteFooter.tsx: mobile (<768px) grid centers every column
(justify-items-center + text-center, reverting at md), brand block centers
as a unit, contact rows stack icon-above-text centered (desktop row layout
restored at md). Company name verified "Terravian Landscaping" (no LLC).
Rendered @390: all 14 sampled elements (headings, links, address rows,
tagline, coverage line) center-offset 0px; overflow 0. Desktop @1440
unchanged (left-aligned columns, offsets intact).

## Step 8 — Homepage hero quality
The live hero was confirmed as a crop of the client master Hero1.jpeg
(3228×2161 — building, lawn, red tulips, beds, blue sky). Re-rendered the
full srcset from that original at 16:9, q74 LANCZOS: 640/1080/1920 plus a
NEW 2560w variant (713 KB) so large displays get real pixels instead of an
upscaled 1920. srcset ladder extended for this slide only; sizes="100vw"
unchanged; object-fit/positions unchanged; no sharpening/recoloring.
Rendered: 1920px viewport → 1920w file; 2560px viewport → 2560w file.
Mobile art-directed 828×1472 crop untouched.

## Step 9 — Homepage hero overlay buttons removed
HeroRotator.tsx: the desktop "Request a Proposal" + "Call 475-347-4090"
overlay strip deleted (orphaned Link/PhoneIcon imports removed). Carousel
controls verified present: desktop counter + prev/pause/next (3 buttons),
mobile 4 slide dots + swipe. No replacement text/buttons; sr-only H1 kept
for SEO/a11y. Other homepage CTAs untouched.

## Step 10 — Site-wide hero brightness audit
Audited all hero-bearing templates (home rotator, ServiceHero ×4, About,
How We Work, Industries) for scrims: the only full-bleed dark overlay is
ServiceHero's bg-charcoal/45, which exists solely for text contrast on
desktop (mobile heroes are already clean images); the snow page adds an
atmospheric Snowfall layer (not a darkening scrim). With Step 9 removing
the homepage's overlay strip + its bg-charcoal/45 backing panel, the
remaining scrims are functional, not decorative — reducing them further
would break text legibility. Sources verified bright: the re-rendered
landscaping hero is materially brighter than the previous encode; other
hero assets are bright daylight/dusk photography at q74. No images
replaced, no filters added, no layouts changed.

## Gates
tsc clean; eslint 0 errors; build OK; sweep 126/126 (0 overflow, 0 errors).

Evidence: tests/results/v38-steps-7-10/.
