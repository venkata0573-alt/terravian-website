# v33 — Landscaping: "Built for" block removal (2026-09-07)

Client brief ("Step 2 — Remove 'Built For' from Landscaping"): on the
Landscape & Maintenance page, remove the entire Built for block — the
audience paragraph, the three industry tags (Hotels & Hospitality /
Property Management Companies / Retail & Commercial Buildings), the
See How We Work link, and its divider/padding/container — leaving no gap.

## Change (1 file)

app/services/[service]/page.tsx — deleted the 34-line grid block inside the
landscaping "What we handle" section (border-t-2 divider + Built for eyebrow
+ whoItsFor paragraph + crossIndustries chip list + See How We Work link).
The same whoItsFor/crossIndustries content intentionally remains on the
snow and hardscape pages' shared handle section (different block — brief
scoped to landscaping only). No variables orphaned.

No spacing patch needed: the capability explorer is now the section's last
child and the section's own bottom padding supplies the rhythm (96px
desktop / 64px mobile measured).

## Validation (production build, rendered)

- tsc clean; eslint 0 errors; build OK.
- Rendered @1440 + @390 on /services/commercial-landscaping: "Built for"
  absent, HOAs paragraph absent, no /how-we-work link inside the handle
  section, last child is the capability explorer, 96/64px bottom padding,
  0 overflow.
- Screenshots confirm the section ends on the explorer and flows directly
  into "The work, visibly" — no empty gap.
- Full sweep: 126/126 (14 routes × 9 widths), 0 overflow, 0 errors.

Evidence: tests/results/v33-built-for/.
