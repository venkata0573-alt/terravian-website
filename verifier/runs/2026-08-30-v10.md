# v10 run log — 2026-08-30 — How We Work redesign

## Brief
User: rethink /how-we-work from UX/persuasion/visual-storytelling. Premium,
confident, warm, trustworthy; fewer words; benefits visible before copy;
commercial (not residential) positioning; no card-rows, no accordions, no
text walls, no SaaS UI, no cheesy graphics; keep brand + header/footer.

## What was removed / transformed
- **Removed**: StageTimeline (accordion rail + detail panel, ~10 expandable
  steps), OutcomeCards (4 identical cards with accordions). Files deleted.
- **Condensed**: ten internal steps → five stages, one sentence each, with a
  "You receive" deliverable per stage (the persuasion column).
- **Transformed**: programs from hidden-behind-accordion → ruled index rows
  (name + one line, ™ first use). Calendar kept (it was already the right
  proof) and reframed with a winter-ops photo pairing.
- **New narrative**: WorkHero (promise + 4 ruled commitments) → ShortVersion
  (charcoal band: fewer complaints / no surprises / proof not promises) →
  ServiceLoop (scroll-filled rail, ghost numerals, loop-closure line) →
  ProofOfWork (photo + overlapping sample record) → YearRound (calendar +
  snow loader photo) → PanoBreak (lakeside campus full-bleed) →
  ProgramsIndex → CTA.

## Verification (Playwright, production build on :3100)
- Build: tsc clean; 27/27 routes; /how-we-work 1.9 kB (156 kB first load).
- Overflow: 0 at 1440 / 390 / 360. Broken images: 0 on all three.
- Headings: single h1 ("You'll never have to chase us."), h2 sequence intact
  incl. footer. Stages are a real <ol>; calendar table semantics untouched.
- Rail: transform matrix scaleY 0.989 mid-scroll (fills with scroll);
  static full under prefers-reduced-motion (useReducedMotion).
- Screenshots reviewed: d-hero, d-short, d-loop, d-proof, d-calendar,
  d-pano, d-end, m-hero, m-loop, m-proof(2), m-calendar, m-pano, m-end.
- Regression: /, /industries, /services, /request-a-proposal → 200,
  overflow 0; home hero visually unchanged; MaintenanceCalendar imported
  exactly once (YearRound).
- Integrity audit: no stats, client names, logos, testimonials, or awards
  added. Service record card labeled "Sample format", blank dotted fields
  only. Programs use nameTrademarked (first use on page).

## Criteria result: 10/10 pass (see verifier/v10/criteria.md)

## Psychology notes (for the record)
- Hotel GM: sees documentation + year-round accountability before any
  service list; the sample record answers "how will I know work happened?"
- Property manager: "fewer complaints / no surprises" names his actual job
  pain in the second band; the "You receive" column shows artifacts, not
  adjectives.
- HOA manager: calendar proves continuity; programs index reads like the
  bylaw-friendly documentation a board wants in the agreement.
