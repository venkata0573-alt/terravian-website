# v11 run log — 2026-08-31 — How We Work polish pass

## Method
Full-page segment audit at 1440 (8 segments) + 390 (11 segments) BEFORE
changes; issue list; two refinement passes; re-audit after each; final gates.

## Issues found → fixes (pass 1)
1. **Hero**: left column was vertically centered → void above eyebrow and a
   ~190px dead zone before the commitments rule. → `lg:items-start` +
   `lg:pt-6` (eyebrow aligns exactly with image top edge); rule margin
   mt-14/md:mt-20 → mt-12/md:mt-16.
2. **Loop**: "YOU RECEIVE" label repeated 5× in a detached right column;
   values wrapped to 2 lines. → restructured rows to 2-col; receive is now
   one inline line under each stage: earth caps label + bold value.
3. **Proof**: ~650px void above the left heading (items-center vs 675px
   right column). → left column is now flex justify-between stretched to
   full height: heading/para top-aligned with photo top, earth-bordered
   quote bottom-aligned with the record card — void becomes deliberate
   editorial whitespace.
4. **Year-round**: items-end pushed the heading low, leaving a void above
   that read as an error. → items-center.
5. **CTA band (global)**: two identical cream-filled buttons competed. →
   new `outlineOnDark` button variant; "Schedule a Property Walkthrough" is
   now outline like the phone link; one filled primary remains.
6. **Footer (global)**: 160px white logo plate read heavy on charcoal. →
   128px (h-32 w-32), tagline spacing mt-4 → mt-5.

## Pass 2
7. **Hero photo**: was 4:3-cropped from a 3:2 source. → `lg:aspect-[3/2]`
   shows the full photograph (no crop) and drops the right column ~70px,
   closing the last hero void. Mobile keeps 4:3.

## Verification (Playwright, production build on :3100)
- tsc clean; 27/27 routes; build 0 errors.
- Overflow: 0 at 1440/390/360. Broken images: 0 on all three.
- Headings: h1 + 7 h2 sequence intact (incl. footer h2s).
- Rail: scaleY 0.925 mid-scroll (normal motion); static under reduced motion.
- Regression: /, /industries, /services, /request-a-proposal → 200,
  overflow 0 (CTA/footer are global components).
- Desktop + mobile full-page segments re-reviewed after each pass.

## Criteria result: 10/10 pass (see verifier/v11/criteria.md)
Stop condition reached: remaining candidates (mobile plate thickness,
footer col-1 whitespace, programs row padding) are preference-level.
