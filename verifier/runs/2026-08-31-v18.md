# Run 2026-08-31 — v18 Web3Forms lead delivery

Build: production (`npm run build`, tsc clean), runner :3100. UI tests in
system chromium; direct API probes via curl/fetch.

## Results

| # | Criterion | Result |
|---|-----------|--------|
| 1 | Brevo purged | PASS — zero matches in app/components/lib/content (docs history in verifier/ left as append-only record) |
| 2 | Chain order | PASS — Web3Forms first (key in code, env-overridable); comments + .env.example updated |
| 3 | Payload shape | PASS — access_key, subject, from_name, replyto (customer email), name, all labeled rows |
| 4 | Contact UI delivery | PASS — success banner "Thank you — your message is on its way to our office…", fields cleared; real email accepted by Web3Forms |
| 5 | Proposal UI delivery | PASS — success banner "Thank you — your proposal request is on its way…", fields cleared |
| 6 | Loading + dupe guard | PASS — button disabled + "Sending…" observed in flight; second click blocked |
| 7 | Client validation | PASS — empty submit: no /api request fired, error summary shown |
| 8 | Spam guards | PASS — honeypot POST → 400 "couldn't be accepted"; fast POST (<3s) → 400. (First UI test run was itself rejected for finishing in <3s — the guard working as designed; paced runs pass.) |
| 9 | Build | PASS — tsc clean, next build clean |

## Live-delivery evidence

Direct POST /api/contact with valid payload →
`{"ok":true,"message":"Thank you — your message is on its way to our office…"}`
meaning the Web3Forms API returned success:true for the client key. Three
test submissions (1 API probe + 2 UI runs, all clearly marked TEST) are
en route to the inbox bound to the access key. Inbox arrival itself must be
confirmed by the client — the sandbox cannot read their mailbox.

Verdict: 9/9 PASS.
