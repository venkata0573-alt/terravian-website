# Run log — v20 — 2026-09-03

## Code changes
- lib/leadDelivery.ts rewritten: relay transports (webhook, Resend,
  MailerSend, Formspree, FormSubmit) and deliverLead/resolveTransports
  REMOVED. Remains: WEB3FORMS_ENDPOINT, WEB3FORMS_ACCESS_KEY (client key),
  WEB3FORMS_TIMEOUT_MS=15000, buildLeadSubmission (form-specific subjects,
  replyto=customer email), isWeb3FormsAccepted (HTTP-ok AND success:true).
- app/api/contact/route.ts, app/api/proposal/route.ts DELETED (git rm).
  POST /api/contact now returns 404 (verified: curl → 404). Only
  /api/address-suggest (address autocomplete, no email) remains under /api.
- components/forms/useFormSubmission.ts reworked: kind-based signature;
  local validation → local spam rejection (honeypot + 3s, BEFORE any
  request) → browser POST FormData to api.web3forms.com with 15 s
  AbortController timeout → success banner + field clearing only on
  isWeb3FormsAccepted(res.ok, json); otherwise retry error, entries kept.
  Slug→label display mapping moved client-side (PROPERTY_TYPE_LABELS,
  services). Duplicate-submit guard added at the top of submit().
- ContactForm/ProposalForm: call-site signature change + comment updates
  only — no markup/design changes.
- .env.example rewritten: Web3Forms-only, no relay variables.
- Toolchain: eslint@9 + eslint-config-next@15.5.22 devDependencies,
  eslint.config.mjs (next/core-web-vitals + next/typescript), lint and
  test scripts in package.json. Two stale eslint-disable comments removed.
- tests/ added: unit (20 tests) + e2e audit script + README + results.

## Verification runs (all against the production build on :3100)
- grep audit: no BREVO/brevo, no formsubmit/formspree/resend/mailersend/
  lead_webhook/MAIL_FROM/LEAD_EMAIL/deliverLead references in source
  (only comments in leadDelivery.ts stating their deliberate absence).
- npx eslint . → exit 0, 0 errors, 3 warnings (all pre-existing
  intentional `<img>` usage in components/home/BeforeAfter.tsx).
  Output: tests/results/lint-output.txt
- npx tsc --noEmit → exit 0. Output: tests/results/typecheck-output.txt
- npm run test:unit → 20/20 pass. Output: tests/results/unit-tests-output.txt
- npm run build → compiled, 25/25 pages, standalone server produced.
  Output: tests/results/production-build-output.txt
- E2E audit (tests/e2e/forms-audit.js, real Chromium, no mocks): three
  full runs, final two consecutive runs 74/74 PASS (exit 0). Covers both
  forms × desktop/mobile: 0px overflow, honeypot present+hidden, empty
  submit 0 requests + field errors, invalid email 0 requests, honeypot
  rejected locally 0 requests + data kept, fast submit 0 requests, valid
  submit exactly 1 POST to api.web3forms.com/submit with key/replyto/
  subject, 0 requests to any relay or legacy API, failure → retry banner
  + entries kept + button re-enabled, double-click → 1 request,
  "Sending…"/disabled/aria-busy mid-flight, no page errors.
  Output: tests/results/e2e-output.txt, e2e-results.json, screenshots/.
- Web3Forms edge response from this environment documented:
  tests/results/web3forms-edge-response.txt (HTTP 403 — datacenter-IP
  block; identical behavior to browser fetch, net::ERR_FAILED in trace).

## Flakiness fixed during the audit
- v1 of the E2E counter flagged address-autocomplete lookups and
  scroll-triggered image lazy-loads as "requests" → introduced
  submissionRequests() (cross-origin or non-suggest /api calls only).
- Next.js route announcer (p[role=alert]) could satisfy banner waits
  mid-flight → banner selectors now match the form's own banners by text.

## Boundary
Live Web3Forms acceptance unobservable from this sandbox (edge block).
Everything up to the edge + the full failure path verified live; the
acceptance gate unit-tested. See verifier/v20/criteria.md.
