# v9 run — 2026-08-30 — circle-mark intro + tagline heading

Client requests: (1) intro loader must rotate the circular mark only, not
the square lockup; (2) homepage promise heading "We show up. That's the
whole trick." → "The Company That Shows Up."

## Changes
- New asset public/images/brand/terravian-mark-circle.png (from client
  upload icononly_nobuffer(1).png): opaque white corners cut with a
  feathered circular alpha mask (2px edge), resized to 640px, 85 KB.
- IntroLoader: square white badge removed; the bare circle turns directly
  on the cream field with a soft drop shadow. Same timing/session/motion
  rules as v8.
- app/page.tsx promise heading → "The Company That Shows Up." (serif
  italic forest accent on "Shows Up."). Supporting line unchanged.

## Gates
- tsc --noEmit clean; production build clean (27/27).
- Intro verified on desktop + mobile: circle mark rotating at center,
  overlay self-dismisses, once per session.
- Home mobile: 0 overflow, 0 broken images.
