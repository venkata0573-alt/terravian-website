# Run 2026-08-24 — v5 client brand logo rail

Build: Next.js 15.5.22 production (standalone), `tsc --noEmit` clean,
`next build` clean (0 errors).

## Checks

| Check | 1920 | 1440 | 390 |
|---|---|---|---|
| Horizontal overflow | 0px | 0px | 0px |
| Broken images (homepage) | 0 | 0 | 0 |
| Rail logo imgs rendered | 28 (14×2 halves) | 28 | 28 |
| All imgs complete, naturalWidth>0 | yes | — | — |

- Visual: white tiles uniform height, hairline borders, sharp corners,
  tiered logo sizing; drift matches field-rail motion; disclaimer line
  present; heading/intro updated.
- Assets: 14 WebP files in public/images/brands/, autocropped to content,
  8–45 KB each.

## Incidents during run

1. Lazy-loaded marquee tiles painted blank in captures → switched rail to
   loading="eager" (14 small local files; duplicates cached).
2. False overflow alarm: a stale `next-server` (PID 961, renamed process
   title) kept port 3100 while /tmp/runner was replaced — old HTML +
   missing old CSS hash (400) rendered the page unstyled. Killed by PID,
   clean restart, CSS 200, overflow back to 0. Pattern kills must target
   the renamed `next-server` title, not `node server.js`.

## Result

PASS — ready for version snapshot.

## Amendment 2026-08-24 (v5a) — section heading rework

User request: replace "Trusted on commercial properties" with a
"properties we proudly serve" direction, creative latitude granted.

- Eyebrow: "In good company"; title: "Properties we *proudly* serve" with
  "proudly" in Newsreader italic serif, forest green (SectionHeading `title`
  widened from `string` to `ReactNode` — backwards compatible).
- Intro rewritten around the real client roster.
- Verified: overflow 0px, broken images 0 at 1920/390; type-check and
  build clean. Screenshot review PASS on both viewports.

## Amendment 2026-08-24 (v5b) — snow card image revert

User request: snow division card had drifted to a patio/pool photo;
revert to the wheel-loader action shot.

- New asset: public/images/portfolio/snow/snow-loader-card.avif — the
  original loader photograph with a soft feathered darkening (max ~64%
  strength, smoothstep, bottom-left weighted) baked into the lower
  foreground so the shadow-only white text stays crisp over bright snow.
  Original snow-loader-pusher-lot.avif untouched.
- DivisionCards snow entry reverted to the loader (alt updated).
- Verified: overflow 0px, broken 0 at 1920/390; desktop + mobile
  screenshots reviewed — text legible, photography dominant.
