# v43 run record — 2026-09-09

Request: client reported the header wordmark still did not match the brand-guideline
reference (textonly(2).png) and mandated a new process: validate locally against the
exact reference image BEFORE touching code.

## Timeline
1. Reference measurement (PIL/numpy, luminance threshold <384 on the white-background
   artwork): TERRAVIAN rows 385–513 (cap 129, width 1072, x103–1174); LANDSCAPING rows
   549–637 (cap 89, width 949, x167–1115); inter-line glyph gap 35px. KEY FINDING:
   line centers 638.5 vs 641.0 → the reference lockup is CENTERED; the shipped header
   was left-aligned. That was the unresolved "same issue".
2. Built /tmp/lab/wordmark.html + render.js (Playwright) + compare.py (metric table +
   scale-aligned pixel IoU + red/green overlays).
3. Lab iteration 1: discovered the lab page itself was falling back to Georgia
   (file:// font blocked under setContent) — fixed with a base64 data-URI @font-face.
   Site code was never affected (production font verified loaded separately).
4. Lab iterations 2–5 (real Cinzel): swept tracking/size/gap. Converged on
   T tracking -0.011em, L 0.95rem / -0.014em, margin-top -1px, centered.
   Achieved vs target: cap ratio 0.689/0.690, width ratio 0.886/0.885,
   gap ratio 0.265/0.271, center delta 0.0019 (≈2px over 1072). Pixel IoU 0.584
   (lab @180px) with stem-width distribution matching reference within ±1px at
   reference scale — residual is rasterization/font-build noise, not layout.
5. Applied the validated parameters to components/layout/SiteHeader.tsx (wrapper →
   flex flex-col items-center; tracking and margin updates). No other file touched.
6. Gates: tsc --noEmit clean; next build OK; runner on :3100; live rendered ink-level
   verification (4× device scale): centered 0–0.5px, cap ratio 0.688–0.724,
   width ratio 0.870–0.893, gap ratio 0.265–0.312, Cinzel 700 loaded; sweep 126/126.

## Notes for future runs
- Playwright page.screenshot intermittently hung "waiting for fonts" this run;
  CDP Page.captureScreenshot is the reliable path.
- The green/brown icon mark pollutes color-keyed ink masks — crop x windows carefully.
- At 16px cap heights, ratio metrics quantize in ~0.02–0.04 steps; validate layout
  parameters at large lab scale and confirm small renders visually.
