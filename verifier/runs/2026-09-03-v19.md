# Run log — v19 — 2026-09-03

## Build
- `npx tsc --noEmit` clean; production build OK; standalone runner on :3100.

## Root-cause probes (before the rework)
- `curl -X POST https://api.web3forms.com/submit` (FormData + JSON, plain and
  with full browser Origin/Referer/User-Agent headers) → HTTP 403,
  `{"success": false, "message": "This method is not allowed. Use our API
  in client side or contact support with server IP address (Pro plan is
  required)"}` — every time, from the sandbox IP.
- `curl -X OPTIONS` preflight probe → same 403 (edge block precedes CORS).
- Web3Forms troubleshooting docs: API expected client-side; server-side
  needs paid plan + IP safelist. Matches observed behavior exactly.

## Real browser test (Chromium, production build, network-monitored)
Sender: venkata0573@gmail.com (real value, typed into the form).
- Contact form: filled all fields (name, company, email, phone, address,
  property type, message), 4.5 s human dwell, submit clicked.
  Network trace, in order:
  1. `POST /api/contact` → 200 `{"ok":true,"mode":"web3forms","submission":
     {"accessKey":"9647ddb3-…","fields":{"Name":"Venkata Reddy",…}}}`
  2. `POST https://api.web3forms.com/submit` fired from the browser
     (FormData) → blocked at the Web3Forms edge from this datacenter IP
     (console: "blocked by CORS policy: No 'Access-Control-Allow-Origin'
     header"; the underlying response is the 403 server-side block page).
  3. Relay fallback `POST /api/contact?relay=1` → 200 ok:true.
  4. Success banner: "Thank you — your message is on its way to our
     office…" — fields cleared.
- Proposal form (/request-a-proposal): same pattern — `/api/proposal` 200
  mode:web3forms → browser POST to Web3Forms → edge-blocked →
  `/api/proposal?relay=1` 200 → banner "Thank you — your proposal request
  is on its way to our office…" — fields cleared.
- Zero formsubmit.co calls fired from the browser; delivery in this
  environment happens only through the server relay (FormSubmit chain) —
  which is why the two test emails from this run arrived FormSubmit-branded.
- Spam guards: honeypot POST → 400, fast POST (100 ms fill) → 400, and the
  network trace shows NO Web3Forms and NO relay call for either.

## Direct in-page fetch probe
`fetch('https://api.web3forms.com/submit', {method:'POST', body: FormData})`
from the /contact page context → TypeError: Failed to fetch + CORS console
error. Documents that the sandbox IP cannot complete the Web3Forms leg
regardless of code path.

## Regression sweep (production build)
- /contact and /request-a-proposal at 1440×900 and 390×844: horizontal
  overflow 0, honeypot input present, all fields render, console clean.
- Empty submit: validation errors shown, ZERO API requests fired (both
  forms, both viewports).

## Conclusion
Architecture verified to the Web3Forms edge: browser posts first (the only
free-plan path), correct key/reply-to/subject/from-name, success UI gated
on Web3Forms acceptance, relay fallback preserves the lead otherwise,
spam guards precede any delivery. The one unverifiable-from-sandbox step
is Web3Forms' own acceptance, blocked environmentally (datacenter IP).
First real-visitor submission after the Vercel deploy is the final
confirmation; the client should expect a Web3Forms-branded email then.
