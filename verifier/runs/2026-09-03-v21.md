# Run log — v21 — 2026-09-03 (responsive UX & content correction)

## Changes
- components/home/HeroRotator.tsx — mobile (<md) content panel REMOVED
  (eyebrow, headline, paragraph, duplicated CTA buttons). Mobile hero is
  now clean photography + minimal slide dots on a localized bottom scrim.
  Desktop/tablet hero (CTA strip, counter, controls) untouched. The fixed
  MobileActionBar (Call Now → tel, Request a Proposal →
  /request-a-proposal) already existed site-wide and is unchanged.
- components/portfolio/Lightbox.tsx — DELETED.
- components/portfolio/PortfolioGrid.tsx — cover button → semantic Link
  to /portfolio/{slug}; lightbox state/focus-restoration removed.
- components/services/ServiceGallery.tsx — thumbnails → static figures
  (no click behavior; already on their destination page).
- Copy corrections (exact client text): app/page.tsx divisions intro;
  components/home/PortfolioFeature.tsx system sentence;
  components/how-we-work/WorkHero.tsx accountable-partner paragraph;
  components/how-we-work/ProofOfWork.tsx evidence paragraph;
  components/how-we-work/YearRound.tsx heading "The year has no empty
  months." removed (section label moved to the eyebrow id), paragraph
  replaced.
- components/services/HeroDrift.tsx — pre-existing hydration defect
  FIXED: markup branched on useReducedMotion() → React #418 for
  reduced-motion visitors on /services/commercial-landscaping. Now
  identical DOM server/client; reduced motion stills the animation via
  the app-wide MotionConfig.

## Verification (all against the production build on :3100)
- npx tsc --noEmit → clean.
- npx eslint . → exit 0 (3 pre-existing <img> warnings, unchanged).
- npm run test:unit → 20/20 pass.
- npm run build → success, 25/25 pages.
- Responsive sweep (tests/e2e pattern, real Chromium, reduced-motion
  context): 14 pages × 9 widths (320/375/390/430/768/1024/1366/1440/1920)
  = **755/755 PASS** — overflow 0 everywhere, no broken images, zero
  lightbox triggers, console clean (incl. the former #418 page), mobile
  action bar present + never overlapped, mobile hero has no visible CTA
  links, desktop/tablet hero CTAs retained, headings within size bounds.
  Results: tests/results/v21-ux/sweep-results.json.
- Functional checks 15/15: cover-image click navigates to the correct
  case study (URL verified), case-study page renders heading + CTA,
  gallery figures non-clickable, mobile bar hrefs correct, all six copy
  corrections render verbatim, both forms intact with zero network
  requests on empty submit, real-keyboard Tab gives a visible 3px focus
  outline on every focusable including card image links.
- Evidence screenshots (16 + focus state): tests/results/v21-ux/*.png —
  homepage + portfolio + snow service + how-we-work at
  375/768/1366/1440, plus 390px hero viewport shot.
- Image→destination map: verifier/v21/image-destination-map.md.

## Notes
- Sweep tooling lessons: networkidle + lazy images require a patient
  scroll pass with a bounded image-settle wait; background sweep
  processes must be single-instance (a zombie overwrote the first run's
  artifacts; re-ran clean, same 755/755 result).
- node_modules on the output mount cannot be deleted (root-owned, busy);
  build staging copies it to /tmp/node_modules (symlink resolution over
  the portal mount is unreliable — caused a transient webpack failure).
